name: Package Test App

on:
  workflow_dispatch:

jobs:
  package:
    permissions: write-all
    runs-on: [self-hosted, windows]
    environment: dev
    env:
      UE_PATH: "D:\\p4\\Release-5.3"
      OUT_PATH: "${{ github.workspace }}\\..\\Output"
      LOG_PATH: "${{ github.workspace }}\\..\\UELOGS"
    steps:
      - name: "Checkout GitHub Action - TestApp"
        uses: actions/checkout@v3
        with:
          clean: "false"
      - name: "Remove existing artifacts"
        run: |
          if (Test-Path ${{ env.OUT_PATH }}) { Remove-Item -Recurse -Force ${{ env.OUT_PATH }} }
      - name: Read the project version
        id: getversion
        run: |
          echo "version=$(BuildTools\GetProjectVersion.ps1)" >> $env:GITHUB_OUTPUT
      - name: "Build TestApp Shipping"
        run: >
          cmd /c 'set uebp_EngineSavedFolder=${{ env.LOG_PATH }}&&
          set uebp_LogFolder=${{ env.LOG_PATH }}&&
          "${{ env.UE_PATH }}\Engine\Build\BatchFiles\RunUAT.bat"
          BuildCookRun
          -project="${{ github.workspace }}\TestApp.uproject"
          -build
          -cook
          -stage
          -archive
          -archivedirectory="${{ env.OUT_PATH }}\\Shipping"
          -package
          -pak
          -targetplatform=Linux
          -target=TestApp
          -clientconfig=Shipping
          -nodebuginfo
          -nop4'
      - name: Archive Shipping build
        uses: a7ul/tar-action@v1.1.0
        with:
          command: c
          cwd: '${{ env.OUT_PATH }}/Shipping'
          files: |
            Linux
          outPath: '${{ env.OUT_PATH }}/${{ steps.getversion.outputs.version }}-${{ github.ref_name }}-${{ github.event.repository.name }}-Linux.tar.gz'
      - name: Run deploy playbook
        uses: dawidd6/action-ansible-playbook@v2
        with:
          # Required, playbook filepath
          playbook: 'deploy_playbook.yml'
          # Optional, directory where playbooks live
          directory: 'BuildTools/'
          # Optional, SSH private key
          key: ${{secrets.DEPLOY_SSH_PRIVATE_KEY}}
          # Optional, literal inventory file contents
          inventory: ${{secrets.DEPLOY_INVENTORY}}
          # Optional, additional flags to pass to ansible-playbook
          options: |
            --u ubuntu
      # - name: "Make the release"
      #   uses: ncipollo/release-action@v1
      #   with:
      #     tag: "${{ steps.getversion.outputs.version }}-${{ github.ref_name }}-${{ github.event.repository.name }}"
      #     artifacts: "${{ env.OUT_PATH }}/${{ steps.getversion.outputs.version }}-${{ github.ref_name }}-${{ github.event.repository.name }}-Linux-Development.tar.gz, ${{ env.OUT_PATH }}/${{ steps.getversion.outputs.version }}-${{ github.ref_name }}-${{ github.event.repository.name }}-Linux.tar.gz"
      #     generateReleaseNotes: true
      #     allowUpdates: true
